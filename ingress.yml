apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: labs-dvn-mini-curso-devops-e-aws-eks-ingress
  annotations:
    alb.ingress.kubernetes.io/load-balancer-name: labs-dvn-mini-curso-eks-alb
    alb.ingress.kubernetes.io/scheme: internet-facing
    alb.ingress.kubernetes.io/subnets: subnet-0217c1cdca419d621,subnet-0b944888fe9e39140
    alb.ingress.kubernetes.io/listen-ports: '[{"HTTP": 80}]'
spec:
  ingressClassName: alb
  rules:
  - http:
      paths:
      - path: /backend
        pathType: Prefix
        backend:
          service:
            name: backend
            port:
              number: 80